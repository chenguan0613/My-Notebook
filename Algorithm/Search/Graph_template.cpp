#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
struct Edge{
    int to;
    int weight;
    Edge(int to, int weight): to(to),weight(weight) {}
};
int main(){
    vector<vector<Edge>> graph;
    
    return 0;
}